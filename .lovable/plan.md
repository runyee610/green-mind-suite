## 修改范围
仅改动 `src/components/green-mfg/DeclarationDetailSections.tsx`，覆盖三处展示：

### 1. 单位产品综合能耗（序号1，`isProductRow` 且非取水）
- 表头去掉「引领值」「基准值」两列（行 1408-1409）。
- 对应 `tbody` 中每行去掉「引领值」「基准值」两个 `<td>`（行 1438-1447）。
- `productCfg`（非取水分支）的 `weightLabel` 由 `"权重（吨标煤）"` 改为 `"权重"`（行 1256）。

### 2. 单位产品取水量（序号6，`isWater`）
- 同一张表（1401-1412）在取水场景下也去掉「引领值」「基准值」两列 —— 与上一步相同的表头/表体调整即可覆盖两种情况。
- `productCfg`（取水分支）的 `weightLabel` 由 `"权重（产品取水量 m3）"` 改为 `"权重"`（行 1244）。

### 3. 所有指标顶部元信息去掉「引领值 / 基准值 /」
去掉以下 3 处 `<span>引领值…</span><span>基准值…</span>`，保留「单位」和「权重」：
- 行 1299-1300（非产品行的元信息行）
- 行 1698-1699（分项父级卡片的元信息行）
- （行 1408-1409 已在第 1 步处理）

其余交互、数据结构、编辑弹窗内的引领值/基准值输入保持不变（仅隐藏顶部只读展示与产品表列）。
