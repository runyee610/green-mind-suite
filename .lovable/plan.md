## 补充评价指标权重

按图示要求，仅调整 `src/components/green-mfg/evaluationIndicators.ts` 中三处 `weight` 字段（沿用现有"主行承载权重、子行为 /"的写法）：

| 位置 | 指标 | 当前 weight | 调整为 |
| --- | --- | --- | --- |
| 第 5 项 · id `5-1`（节约原材料 - 减少原辅材料消耗案例） | 主行 | `"/"` | `"8"` |
| 第 5 项 · id `5-2`（节约原材料 - 再生/回收材料案例） | 子行 | `"/"` | 保持 `"/"` |
| 第 13 项 · id `14`（产品碳足迹 - 开展碳足迹核算产品类别占比） | — | `"8"` | `"4"` |
| 第 14 项 · id `14b`（绿色产品 - 节能产品占比*） | — | `"/"` | `"4"` |

不改动其他字段，不涉及业务逻辑/评分算法。UI 会自动在"权重 X"处显示新值，AI 打分等消费 weight 数值处也会同步生效。